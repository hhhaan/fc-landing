---
import '../styles/global.css';
import Favicon from '../components/ui/Favicon.astro';
import OxNavAuth from '../components/ui/OxNavAuth.astro';
import OxNavAuthMobile from '../components/ui/OxNavAuthMobile.astro';
import Seo from '../components/ui/Seo.astro';
// import OxWorkflowPanel from "../components/ui/OxWorkflowPanel.astro";
import { DOCS, DOCS_BASE, FAQ_ITEMS, HARDWARE, MACHINE_BRANDS, PLAN_COPY, PROOFS } from '../content/product';
import { getViewer } from '../lib/authState';
import { getMarketPricing, resolveBillingMarket } from '../lib/market';
import { resolveTrialCta } from '../lib/trialCta';

const billingMarket = resolveBillingMarket(Astro.request);
const pricing = getMarketPricing(billingMarket);

const user = await getViewer(Astro);
const isLoggedIn = !!user;
const trialCta = await resolveTrialCta(Astro);
const year = new Date().getFullYear();

const capabilities = [
    {
        n: '01',
        title: 'Every batch, documented',
        body: 'Log BT/ET/RoR and keep every roast searchable — so the next shift can hit the same cup.',
    },
    {
        n: '02',
        title: 'Built for the roast floor',
        body: 'Modbus, Phidget, and Artisan-ready paths. Keep roasting when the network drops.',
    },
    {
        n: '03',
        title: 'From charge to bag',
        body: 'Day plan, green inventory, and production reports in one place — not five spreadsheets.',
    },
    {
        n: '04',
        title: 'Hand off the shift',
        body: 'Promote winning profiles, leave notes, and give the next roaster a clear reference — not a verbal handoff.',
    },
];

const pains = [
    {
        n: '01',
        title: 'The cup drifts between shifts',
        body: 'Profiles live in someone’s head, a USB stick, or a spreadsheet nobody updates.',
    },
    {
        n: '02',
        title: 'The floor and the office don’t talk',
        body: 'Live curves on one machine. Inventory and orders somewhere else.',
    },
    {
        n: '03',
        title: 'Browser tools freeze when it matters',
        body: 'You need low-latency hardware links and an app that still works offline.',
    },
];

const proofs = PROOFS;

const steps = [
    {
        n: 'I',
        title: 'Connect your machine',
        body: 'Pair sensors and controllers. Modbus, Phidget, and Artisan-ready setups work out of the box.',
    },
    {
        n: 'II',
        title: 'Roast with a reference',
        body: 'Load a profile, mark events, and follow the curve with live readings on the floor.',
    },
    {
        n: 'III',
        title: 'Save and improve',
        body: 'Weights, phases, and notes save automatically. Promote winners for the next shift.',
    },
];

const hardware = HARDWARE;

const marqueeItems = [
    'Live BT / ET / RoR',
    'Profile library',
    'Batch history',
    'Modbus',
    'Phidget',
    'Artisan-ready',
    'Probat · Giesen · Diedrich',
    'Roast planner',
    'Inventory',
    'Production reports',
    'Team sync',
    'macOS',
    'Windows',
];

const metaTitle = 'First Crack — Coffee Roasting Software: Roast Profiles, Inventory & Roastery ERP';
const metaDescription = `Roast profiling, green coffee inventory, and production ERP for specialty roasteries. Live BT/ET/RoR curves, Modbus/Phidget/Artisan machine connect, offline desktop app for macOS & Windows. ${PLAN_COPY.trialBadge} · Pro from ${pricing.heroProMonthly}/mo.`;

const structuredData = {
    '@context': 'https://schema.org',
    '@graph': [
        {
            '@type': 'SoftwareApplication',
            name: 'First Crack',
            applicationCategory: 'BusinessApplication',
            operatingSystem: 'macOS, Windows',
            description: metaDescription,
            url: 'https://firstcrackiscoming.com/',
            offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
        },
        {
            '@type': 'Organization',
            name: 'First Crack',
            url: 'https://firstcrackiscoming.com/',
            logo: 'https://firstcrackiscoming.com/favicon.ico',
        },
    ],
};
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <Favicon />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={metaDescription} />
    <title>{metaTitle}</title>
    <Seo title={metaTitle} description={metaDescription} />
    <link
      rel="alternate"
      hreflang="x-default"
      href="https://firstcrackiscoming.com/"
    />
    <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Instrument+Sans:ital,wght@0,400..700;1,400..700&family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="ox">
    <main class="ox-main noise-overlay">
      <!-- ── Header ───────────────────────────────────────── -->
      <header class="ox-header" id="ox-header">
        <div class="ox-scroll-progress" id="ox-scroll-progress" aria-hidden="true"></div>
        <nav class="ox-nav">
          <a href="/" class="ox-brand">
            <span class="ox-brand-name">First Crack</span>
          </a>

          <div class="ox-nav-links">
            <a href="#features" class="ox-nav-link" data-section="features">Product</a>
            <a href="#how-it-works" class="ox-nav-link" data-section="how-it-works">How it works</a>
            <a href="#platform" class="ox-nav-link" data-section="platform">Platform</a>
            <a href="#pricing" class="ox-nav-link" data-section="pricing">Pricing</a>
            <a href="https://docs.firstcrackiscoming.com" class="ox-nav-link">Docs</a>
          </div>

          <div class="ox-nav-end">
            <OxNavAuth user={user} variant="marketing" />
            <button
              type="button"
              class="ox-burger"
              aria-label="Toggle menu"
              aria-expanded="false"
              aria-controls="ox-mobile"
            >
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
                <line class="ox-burger-line" x1="4" y1="6" x2="20" y2="6"></line>
                <line class="ox-burger-line" x1="4" y1="12" x2="20" y2="12"></line>
                <line class="ox-burger-line" x1="4" y1="18" x2="20" y2="18"></line>
              </svg>
            </button>
          </div>
        </nav>

        <div id="ox-mobile" class="ox-mobile">
          <div class="ox-mobile-inner">
            <div class="ox-mobile-links">
              <a href="#features" class="ox-nav-link" data-section="features">Product</a>
              <a href="#how-it-works" class="ox-nav-link" data-section="how-it-works">How it works</a>
              <a href="#platform" class="ox-nav-link" data-section="platform">Platform</a>
              <a href="#pricing" class="ox-nav-link" data-section="pricing">Pricing</a>
              <a href="https://docs.firstcrackiscoming.com">Docs</a>
              <a href="/download">Download app</a>
            </div>
            <div class="ox-mobile-cta">
              <OxNavAuthMobile user={user} />
            </div>
          </div>
        </div>
      </header>

      <!-- ── Hero ─────────────────────────────────────────── -->
      <section class="ox-hero ox-pane">
        <div class="ox-grid-bg" aria-hidden="true"></div>

        <div class="ox-shell ox-hero-inner">
          <div class="ox-hero-copy">
            <div class="ox-eyebrow reveal">
              <span class="ox-line"></span>
              Desktop roasting software
            </div>

            <h1 class="ox-hero-title reveal reveal-d1">
              <span class="block">Roast with data.</span>
              <span class="block ox-hero-title-muted">Hit the same cup again.</span>
            </h1>

            <p class="ox-hero-lead reveal reveal-d2">
              When profiles live in heads and spreadsheets, the cup drifts.
              First Crack logs every batch, connects your machine, and keeps
              the next shift on the same curve — on the floor, not in a browser tab.
            </p>

            <div class="ox-hero-trust reveal reveal-d2">
              <span>{PLAN_COPY.trialHeroTrust}</span>
              <span>Pro from <strong>{pricing.heroProMonthly}</strong>/mo · Free {PLAN_COPY.freeFeatureBatches}</span>
            </div>

            <div class="ox-hero-actions reveal reveal-d3">
              <a href={trialCta.href} class="ox-btn ox-btn-solid ox-btn-lg ox-hero-cta-primary">
                {trialCta.label}
              </a>
              <a href="/download" class="ox-btn ox-btn-outline ox-btn-lg">
                Download app
              </a>
            </div>

            <p class="ox-hero-fine reveal reveal-d3">
              Try free → connect your machine → keep what works · macOS &amp; Windows
            </p>
          </div>

          <div class="ox-hero-visual" aria-hidden="true">
            <div class="ox-hero-glow"></div>
            <canvas id="ox-hero-canvas"></canvas>
          </div>
        </div>

        <!-- Stats marquee -->
        <div class="ox-marquee" aria-hidden="true">
          <div class="ox-marquee-track">
            {
              [...marqueeItems, ...marqueeItems].map((item) => (
                <span class="ox-marquee-item">
                  <span class="ox-marquee-dot"></span>
                  {item}
                </span>
              ))
            }
          </div>
        </div>
      </section>

      <!-- ── Proof strip ──────────────────────────────────── -->
      <section class="ox-proof" aria-label="Product facts">
        <div class="ox-shell">
          <ul class="ox-proof-list">
            {
              proofs.map((p) => (
                <li class="ox-proof-item">
                  <span class="ox-proof-label">{p.label}</span>
                  <span class="ox-proof-detail">{p.detail}</span>
                </li>
              ))
            }
          </ul>
        </div>
      </section>

      <!-- ── Pain ─────────────────────────────────────────── -->
      <section class="ox-section ox-pain" id="why">
        <div class="ox-shell">
          <div class="ox-section-head">
            <div class="ox-eyebrow reveal">
              <span class="ox-line"></span>
              Why First Crack
            </div>
            <h2 class="ox-h2 reveal">
              Built for roasteries that
              <br />
              <span class="ox-muted">can’t afford a drifting cup.</span>
            </h2>
            <p class="ox-lead reveal">
              Consistency dies in the gaps — between shifts, between tools, between the floor and the office.
            </p>
          </div>

          <div class="ox-pain-grid">
            {
              pains.map((p, i) => (
                <article class="ox-pain-card reveal" style={`--d: ${i * 60}ms`}>
                  <span class="ox-pain-n">{p.n}</span>
                  <h3 class="ox-pain-title">{p.title}</h3>
                  <p class="ox-pain-body">{p.body}</p>
                </article>
              ))
            }
          </div>
        </div>
      </section>

      <!-- ── Capabilities ─────────────────────────────────── -->
      <section class="ox-section ox-pane" id="features">
        <div class="ox-shell">
          <div class="ox-section-head">
            <div class="ox-eyebrow reveal">
              <span class="ox-line"></span>
              Product
            </div>
            <h2 class="ox-h2 reveal">
              Everything on the floor.
              <br />
              <span class="ox-muted">Nothing in the way.</span>
            </h2>
          </div>

          <div class="ox-cap-list">
            {
              capabilities.map((c, i) => (
                <article
                  class="ox-cap reveal"
                  style={`--d: ${i * 60}ms`}
                >
                  <div class="ox-cap-num">{c.n}</div>
                  <div class="ox-cap-body">
                    <h3 class="ox-cap-title">{c.title}</h3>
                    <p class="ox-cap-text">{c.body}</p>
                  </div>
                  {/* ponytail: screenshot layer disabled for now — text-first. Restore when real screenshots exist.
                  <div
                    class="ox-cap-media"
                    role="img"
                    aria-label={`Screenshot placeholder — ${c.title}`}
                  >
                    <span class="ox-ph-label">Screenshot</span>
                  </div>
                  */}
                </article>
              ))
            }
          </div>
        </div>
      </section>

      <!-- ── Process (inverted) ───────────────────────────── -->
      <section class="ox-process ox-pane" id="how-it-works">
        <div class="ox-shell">
          <div class="ox-section-head">
            <div class="ox-eyebrow ox-eyebrow-on-dark reveal">
              <span class="ox-line ox-line-on-dark"></span>
              Workflow
            </div>
            <h2 class="ox-h2 ox-h2-on-dark reveal">
              Three steps.
              <br />
              <span class="ox-on-dark-muted">Consistent coffee.</span>
            </h2>
          </div>

          <div class="ox-process-grid">
            <div class="ox-steps">
              {
                steps.map((s, i) => (
                  <button
                    type="button"
                    class:list={["ox-step", i === 0 && "is-active"]}
                    data-step={i}
                  >
                    <span class="ox-step-n">{s.n}</span>
                    <div>
                      <h3 class="ox-step-title">{s.title}</h3>
                      <p class="ox-step-body">{s.body}</p>
                    </div>
                  </button>
                ))
              }
            </div>

            {/* ponytail: mock-UI panel disabled — text-first workflow. Restore with import when ready.
            <OxWorkflowPanel />
            */}
          </div>
        </div>
      </section>

      <!-- ── Platform ─────────────────────────────────────── -->
      <section class="ox-section ox-pane" id="platform">
        <div class="ox-shell ox-platform">
          <div class="ox-platform-copy">
            <div class="ox-eyebrow reveal">
              <span class="ox-line"></span>
              Platform
            </div>
            <h2 class="ox-h2 reveal">
              Built for the
              <br />
              roast floor.
            </h2>
            <p class="ox-lead reveal">
              A native desktop app with low-latency hardware links — and cloud
              sync when you’re back in the office. Connect the gear you already
              own.
            </p>
            <p class="ox-platform-brands reveal">
              Common setups: {MACHINE_BRANDS}.
            </p>
            <div class="ox-stats reveal">
              <div>
                <div class="ox-stat-num">macOS</div>
                <div class="ox-stat-label">&amp; Windows</div>
              </div>
              <div>
                <div class="ox-stat-num">Live</div>
                <div class="ox-stat-label">BT · ET · RoR</div>
              </div>
              <div>
                <div class="ox-stat-num">Cloud</div>
                <div class="ox-stat-label">sync included</div>
              </div>
            </div>
            <div class="ox-platform-actions reveal">
              <a href="/download" class="ox-btn ox-btn-solid ox-btn-lg">
                Download desktop app
              </a>
              <a href={DOCS.machines} class="ox-btn ox-btn-outline ox-btn-lg">
                Machine setup docs
              </a>
            </div>
          </div>

          <ul class="ox-hw-list reveal">
            {
              hardware.map((h) => (
                <li class="ox-hw-item">
                  <span class="ox-hw-tag">{h.tag}</span>
                  <strong>{h.name}</strong>
                  <p>{h.desc}</p>
                </li>
              ))
            }
          </ul>
        </div>
      </section>

      <!-- ── Pricing ──────────────────────────────────────── -->
      <section class="ox-section ox-pricing ox-pane" id="pricing">
        <div class="ox-shell">
          <div class="ox-section-head ox-section-head-center">
            <div class="ox-eyebrow reveal" style="justify-content:center">
              <span class="ox-line"></span>
                            Pricing
            </div>
            <h2 class="ox-h2 reveal">
              Simple pricing
              <br />
              for serious roasters
            </h2>
            <p class="ox-lead ox-lead-center reveal">
              {PLAN_COPY.pricingLead} Need more seats? Enterprise covers teams.
            </p>
          </div>

          <div class="ox-bill reveal">
            <div class="ox-bill-toggle" role="group" aria-label="Billing period">
              <button type="button" class="is-active" id="btn-monthly" aria-pressed="true">
                Monthly
              </button>
              <button type="button" id="btn-yearly" aria-pressed="false">
                Annual <em>Save ~20%</em>
              </button>
            </div>
          </div>

          <div class="ox-price-grid reveal">
            <article class="ox-price-card">
              <div class="ox-price-top">
                <span class="ox-price-n">01</span>
                <h3>Pro</h3>
                <p>Roast, inventory, and schedule — floor essentials</p>
              </div>
              <div class="ox-price-amt">
                {
                  pricing.pro.monthlyAnchor && (
                    <span class="ox-price-anchor" aria-hidden="true">
                      <s>{pricing.pro.monthlyAnchor}</s>
                    </span>
                  )
                }
                <span
                  class="ox-price-num"
                  id="pro-price"
                  data-monthly={pricing.pro.monthly}
                  data-yearly={pricing.pro.yearlyEffective}>{pricing.pro.monthly}</span
                >
                <span class="ox-price-per" id="pro-period" data-monthly="/mo" data-yearly="/mo">/mo</span>
              </div>
              <p class="ox-price-note" id="pro-yearly-note" hidden>{pricing.pro.yearlyTotal}</p>
              <ul>
                <li>{PLAN_COPY.proFeatureBatches}</li>
                <li>Active Roast + history + profiles</li>
                <li>Beans, blends, roast schedule</li>
                <li>1 seat</li>
                <li>Desktop app (macOS &amp; Windows)</li>
              </ul>
              <a id="pro-start-btn" href="/start-pro?plan=pro-monthly" class="ox-btn ox-btn-outline ox-btn-block" data-href-monthly="/start-pro?plan=pro-monthly" data-href-yearly="/start-pro?plan=pro-yearly">
                {PLAN_COPY.trialCtaLabel}
              </a>
            </article>

            <article class="ox-price-card is-featured">
              <div class="ox-price-badge">Unlimited + stores</div>
              <div class="ox-price-top">
                <span class="ox-price-n">02</span>
                <h3>Pro+</h3>
                <p>Unlimited batches + store connectors</p>
              </div>
              <div class="ox-price-amt">
                {
                  pricing.proPlus.monthlyAnchor && (
                    <span class="ox-price-anchor" aria-hidden="true">
                      <s>{pricing.proPlus.monthlyAnchor}</s>
                    </span>
                  )
                }
                <span
                  class="ox-price-num"
                  id="proplus-price"
                  data-monthly={pricing.proPlus.monthly}
                  data-yearly={pricing.proPlus.yearlyEffective}>{pricing.proPlus.monthly}</span
                >
                <span class="ox-price-per" id="proplus-period" data-monthly="/mo" data-yearly="/mo">/mo</span>
              </div>
              <p class="ox-price-note" id="proplus-yearly-note" hidden>{pricing.proPlus.yearlyTotal}</p>
              <ul>
                <li>{PLAN_COPY.proPlusFeatureBatches}</li>
                <li>Everything in Pro</li>
                <li>Shopify &amp; store connectors</li>
                <li>Order sync (Naver, Cafe24, Shopify)</li>
                <li>1 seat</li>
              </ul>
              <a id="proplus-start-btn" href="/start-pro?plan=pro-plus-monthly" class="ox-btn ox-btn-solid ox-btn-block" data-href-monthly="/start-pro?plan=pro-plus-monthly" data-href-yearly="/start-pro?plan=pro-plus-yearly">
                {PLAN_COPY.trialCtaLabel}
              </a>
            </article>

            <article class="ox-price-card">
              <div class="ox-price-top">
                <span class="ox-price-n">03</span>
                <h3>Enterprise</h3>
                <p>Multi-seat teams, custom work, dedicated engineer</p>
              </div>
              <div class="ox-price-amt">
                <span class="ox-price-num">Contact</span>
              </div>
              <ul>
                <li>Unlimited batches + team seats</li>
                <li>Custom features &amp; FDE support</li>
                <li>From $300/mo + $19/seat</li>
                <li>Priority setup help</li>
                <li>Contract &amp; invoice billing</li>
              </ul>
              <a href="mailto:contact@firstcrackiscoming.com?subject=Enterprise%20inquiry" class="ox-btn ox-btn-outline ox-btn-block">
                Contact sales
              </a>
            </article>
          </div>

          <p class="ox-price-footnote reveal">
            {PLAN_COPY.freeShort} after trial if you don’t continue paid. Full matrix on{" "}
            <a href="/faq">FAQ</a> and{" "}
            <a href={DOCS.plans}>plans docs</a>.
          </p>
        </div>
      </section>

      <!-- ── FAQ ──────────────────────────────────────────── -->
      <section class="ox-section ox-faq ox-pane" id="faq">
        <div class="ox-shell">
          <div class="ox-section-head">
            <div class="ox-eyebrow reveal">
              <span class="ox-line"></span>
              FAQ
            </div>
            <h2 class="ox-h2 reveal">
              Straight answers
              <br />
              <span class="ox-muted">before the first charge.</span>
            </h2>
          </div>
          <div class="ox-faq-list">
            {
              FAQ_ITEMS.map((item, i) => (
                <details class="ox-faq-item reveal" style={`--d: ${i * 40}ms`}>
                  <summary>{item.q}</summary>
                  <p>{item.a}</p>
                </details>
              ))
            }
          </div>
          <p class="ox-faq-more reveal">
            More detail in{" "}
            <a href="/faq">all FAQs</a>,{" "}
            <a href={DOCS.machines}>machine docs</a>, or{" "}
            <a href="mailto:contact@firstcrackiscoming.com">email us</a>.
          </p>
        </div>
      </section>

      <!-- ── Final CTA ────────────────────────────────────── -->
      <section class="ox-final">
        <div class="ox-shell ox-final-inner">
          <div>
            <h2 class="ox-h2 ox-final-title reveal">
              Install before
              <br />
              the next charge
            </h2>
            <p class="ox-lead reveal">
              Download the desktop app, connect your machine, and run your first
              trial batch — before the next green coffee hits the hopper.
            </p>
            <div class="ox-hero-actions reveal">
              <a href={trialCta.href} class="ox-btn ox-btn-solid ox-btn-lg">
                {trialCta.label}
              </a>
              <a href="/download" class="ox-btn ox-btn-outline ox-btn-lg">
                Download app
              </a>
            </div>
            <p class="ox-hero-fine reveal">
              {PLAN_COPY.trialFine}
            </p>
          </div>
        </div>
      </section>

      <!-- ── Footer ───────────────────────────────────────── -->
      <footer class="ox-footer">
        <div class="ox-shell">
          <div class="ox-footer-top">
            <div class="ox-footer-brand">
              <a href="/" class="ox-brand">
                <span class="ox-brand-name">First Crack</span>
              </a>
              <p>Desktop roasting software for specialty coffee.</p>
            </div>
              <div class="ox-footer-cols">
              <div>
                <h4>Product</h4>
                <a href="#features">Features</a>
                <a href="#how-it-works">Workflow</a>
                <a href="#pricing">Pricing</a>
                <a href="#faq">FAQ</a>
                <a href="/download">Download</a>
                <a href={DOCS_BASE}>Docs</a>
              </div>
              <div>
                <h4>Company</h4>
                <a href="/about">About</a>
                <a href="/careers">Careers</a>
                <a href="/faq">FAQ</a>
                <a href="mailto:contact@firstcrackiscoming.com">Contact</a>
              </div>
              <div>
                <h4>Account</h4>
                {
                  isLoggedIn ? (
                    <>
                      <a href="/account">Account</a>
                      <form action="/auth/signout" method="POST" class="ox-footer-signout">
                        <button type="submit">Sign out</button>
                      </form>
                    </>
                  ) : (
                    <>
                      <a href="/login">Log in</a>
                      <a href="/signup">Sign up</a>
                    </>
                  )
                }
              </div>
              <div>
                <h4>Legal</h4>
                <a href="/privacy">Privacy</a>
                <a href="/terms">Terms</a>
              </div>
            </div>
          </div>
          <div class="ox-footer-bottom">
            <span>© {year} First Crack</span>
            <span>Built for the roast floor</span>
          </div>
        </div>
      </footer>
    </main>

    <script>
      // Header: scroll progress, compact, hide/show, section spy
      const header = document.getElementById("ox-header");
      const progress = document.getElementById("ox-scroll-progress");
      const navLinks = document.querySelectorAll<HTMLAnchorElement>(
        ".ox-nav-link[data-section]",
      );
      const sections = ["features", "how-it-works", "platform", "pricing"]
        .map((id) => document.getElementById(id))
        .filter((el): el is HTMLElement => el !== null);

      let lastScrollY = window.scrollY;
      const hideThreshold = 40;
      const prefersReducedMotion = window.matchMedia(
        "(prefers-reduced-motion: reduce)",
      ).matches;
      let navScrollRaf = 0;

      const setActiveSection = (id: string | null) => {
        navLinks.forEach((link) => {
          link.classList.toggle("is-active", link.dataset.section === id);
        });
      };

      /** Ease-out quart — close to ox reveal cubic-bezier(0.22, 1, 0.36, 1) */
      const easeOx = (t: number) => 1 - (1 - t) ** 4;

      const scrollToSection = (id: string) => {
        const section = document.getElementById(id);
        if (!section) return;

        closeMobile();
        document.body.classList.add("is-pane-nav", "is-nav-scrolling");
        header?.classList.add("is-hidden");
        setActiveSection(id);

        const target = section.offsetTop;
        const start = window.scrollY;
        const delta = target - start;

        if (navScrollRaf) {
          cancelAnimationFrame(navScrollRaf);
          navScrollRaf = 0;
        }

        const finish = () => {
          document.body.classList.remove("is-nav-scrolling");
          lastScrollY = window.scrollY;
          history.replaceState(null, "", `#${id}`);
          // Replay entrance on target reveals still waiting
          section.querySelectorAll(".reveal:not(.is-in)").forEach((el) => {
            el.classList.add("is-in");
          });
        };

        if (Math.abs(delta) < 2 || prefersReducedMotion) {
          window.scrollTo(0, target);
          finish();
          return;
        }

        const duration = Math.min(1100, Math.max(480, Math.abs(delta) * 0.55));
        const t0 = performance.now();

        const step = (now: number) => {
          const t = Math.min(1, (now - t0) / duration);
          const y = start + delta * easeOx(t);
          window.scrollTo(0, y);
          lastScrollY = y;
          if (t < 1) {
            navScrollRaf = requestAnimationFrame(step);
          } else {
            navScrollRaf = 0;
            finish();
          }
        };
        navScrollRaf = requestAnimationFrame(step);
      };

      navLinks.forEach((link) => {
        link.addEventListener("click", (e) => {
          const id = link.dataset.section;
          if (!id) return;
          e.preventDefault();
          scrollToSection(id);
        });
      });

      // Footer / in-page anchors share the same scroll animation
      document.querySelectorAll<HTMLAnchorElement>('a[href^="#"]').forEach((a) => {
        if (a.dataset.section) return;
        const id = a.getAttribute("href")?.slice(1);
        if (!id || !document.getElementById(id)) return;
        a.addEventListener("click", (e) => {
          e.preventDefault();
          scrollToSection(id);
        });
      });

      const onScroll = () => {
        const y = window.scrollY;
        const maxScroll = document.documentElement.scrollHeight - window.innerHeight;

        header?.classList.toggle("is-scrolled", y > 20);
        header?.classList.toggle("is-compact", y > 80);

        if (progress && maxScroll > 0) {
          progress.style.transform = `scaleX(${Math.min(1, y / maxScroll)})`;
        }

        if (y <= hideThreshold) {
          document.body.classList.remove("is-pane-nav");
          header?.classList.remove("is-hidden");
        } else if (document.body.classList.contains("is-nav-scrolling")) {
          // Programmatic nav jump: keep header hidden until animation ends
          header?.classList.add("is-hidden");
        } else if (y < lastScrollY) {
          // Scroll up → show nav (also clears post-jump lock)
          document.body.classList.remove("is-pane-nav");
          header?.classList.remove("is-hidden");
        } else if (y > lastScrollY) {
          header?.classList.add("is-hidden");
        }

        lastScrollY = y;
      };
      onScroll();
      window.addEventListener("scroll", onScroll, { passive: true });

      if ("IntersectionObserver" in window && sections.length) {
        const visible = new Map<string, number>();
        const spy = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                visible.set(entry.target.id, entry.intersectionRatio);
              } else {
                visible.delete(entry.target.id);
              }
            });

            if (visible.size === 0) return;

            let bestId: string | null = null;
            let bestRatio = 0;
            visible.forEach((ratio, id) => {
              if (ratio > bestRatio) {
                bestRatio = ratio;
                bestId = id;
              }
            });
            setActiveSection(bestId);
          },
          { rootMargin: "-45% 0px -45% 0px", threshold: [0, 0.5, 1] },
        );
        sections.forEach((section) => spy.observe(section));
      }

      // Mobile menu
      const burger = document.querySelector(".ox-burger") as HTMLButtonElement | null;
      const mobile = document.getElementById("ox-mobile");

      const closeMobile = () => {
        burger?.setAttribute("aria-expanded", "false");
        mobile?.classList.remove("is-open");
        header?.classList.remove("is-menu-open");
        document.body.classList.remove("ox-menu-open");
      };

      const openMobile = () => {
        mobile?.classList.add("is-open");
        burger?.setAttribute("aria-expanded", "true");
        header?.classList.add("is-menu-open");
        header?.classList.remove("is-hidden");
        document.body.classList.add("ox-menu-open");
      };

      burger?.addEventListener("click", () => {
        const isOpen = burger.getAttribute("aria-expanded") === "true";
        if (isOpen) closeMobile();
        else openMobile();
      });

      mobile?.querySelectorAll("a:not([data-section])").forEach((a) => {
        a.addEventListener("click", closeMobile);
      });

      document.addEventListener("keydown", (e) => {
        if (e.key === "Escape" && burger?.getAttribute("aria-expanded") === "true") {
          closeMobile();
        }
      });

      window.addEventListener("resize", () => {
        if (window.innerWidth >= 768 && burger?.getAttribute("aria-expanded") === "true") {
          closeMobile();
        }
      });

      // Process step highlight
      document.querySelectorAll(".ox-step").forEach((btn) => {
        btn.addEventListener("click", () => {
          document
            .querySelectorAll(".ox-step")
            .forEach((b) => b.classList.remove("is-active"));
          btn.classList.add("is-active");
        });
      });

      // Pricing toggle
      const btnMonthly = document.getElementById("btn-monthly");
      const btnYearly = document.getElementById("btn-yearly");

      function setBilling(isYearly: boolean) {
        if (!btnMonthly || !btnYearly) return;
        btnMonthly.classList.toggle("is-active", !isYearly);
        btnYearly.classList.toggle("is-active", isYearly);
        btnMonthly.setAttribute("aria-pressed", String(!isYearly));
        btnYearly.setAttribute("aria-pressed", String(isYearly));

        document.querySelectorAll(".ox-price-card").forEach((card) => {
          const num = card.querySelector<HTMLElement>(".ox-price-num");
          const per = card.querySelector<HTMLElement>(".ox-price-per");
          const note = card.querySelector<HTMLElement>(".ox-price-note");
          const cta = card.querySelector<HTMLAnchorElement>("a.ox-btn");
          if (num) {
            num.textContent = isYearly
              ? (num.dataset.yearly ?? num.dataset.monthly ?? "")
              : (num.dataset.monthly ?? "");
          }
          if (per) {
            per.textContent = isYearly
              ? (per.dataset.yearly ?? per.dataset.monthly ?? "")
              : (per.dataset.monthly ?? "");
          }
          if (note) note.hidden = !isYearly;
          const anchor = card.querySelector<HTMLElement>(".ox-price-anchor");
          if (anchor) anchor.hidden = isYearly;
          if (cta) {
            cta.href = isYearly
              ? (cta.dataset.hrefYearly ?? cta.href)
              : (cta.dataset.hrefMonthly ?? cta.href);
          }
        });
      }
      btnMonthly?.addEventListener("click", () => setBilling(false));
      btnYearly?.addEventListener("click", () => setBilling(true));
      setBilling(false);

      // Reveal
      const reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
      if (!reduce && "IntersectionObserver" in window) {
        const io = new IntersectionObserver(
          (entries) => {
            entries.forEach((e) => {
              if (e.isIntersecting) {
                e.target.classList.add("is-in");
                io.unobserve(e.target);
              }
            });
          },
          { threshold: 0.12, rootMargin: "0px 0px -32px 0px" },
        );
        document.querySelectorAll(".reveal").forEach((el) => io.observe(el));
      } else {
        document.querySelectorAll(".reveal").forEach((el) => el.classList.add("is-in"));
      }

      // ── Drum cylinder wireframe ───────────────────────────
      {
        const canvas = document.getElementById("ox-hero-canvas") as HTMLCanvasElement | null;
        if (canvas) {
          const ctx = canvas.getContext("2d");
          if (ctx) {
            const dpr = window.devicePixelRatio || 1;
            let W = 0;
            let H = 0;

            function resize() {
              const rect = canvas!.getBoundingClientRect();
              W = rect.width;
              H = rect.height;
              canvas!.width = W * dpr;
              canvas!.height = H * dpr;
              ctx!.setTransform(dpr, 0, 0, dpr, 0, 0);
            }
            resize();
            window.addEventListener("resize", resize);

            type Pt3 = { x: number; y: number; z: number };
            type Seg = { a: Pt3; b: Pt3; weight: number };

            const R = 1;
            const HALF_H = 0.72;
            const SEGS = 48;
            const RINGS = [-0.72, -0.24, 0.24, 0.72];
            const MERIDIANS = 8;

            function ringAt(y: number): Pt3[] {
              const pts: Pt3[] = [];
              for (let i = 0; i < SEGS; i++) {
                const t = (i / SEGS) * Math.PI * 2;
                pts.push({ x: Math.cos(t) * R, y, z: Math.sin(t) * R });
              }
              return pts;
            }

            const rings = RINGS.map(ringAt);
            const segments: Seg[] = [];

            for (const ring of rings) {
              for (let i = 0; i < SEGS; i++) {
                segments.push({ a: ring[i], b: ring[(i + 1) % SEGS], weight: 1 });
              }
            }

            for (let m = 0; m < MERIDIANS; m++) {
              const i = Math.floor((m / MERIDIANS) * SEGS);
              segments.push({ a: rings[0][i], b: rings[rings.length - 1][i], weight: 1.25 });
            }

            let angleY = 0.4;
            let angleX = 0.42;
            const speedY = 0.0018;

            function rotate(p: Pt3, ax: number, ay: number): Pt3 {
              let x = p.x * Math.cos(ay) - p.z * Math.sin(ay);
              let z = p.x * Math.sin(ay) + p.z * Math.cos(ay);
              const y = p.y * Math.cos(ax) - z * Math.sin(ax);
              z = p.y * Math.sin(ax) + z * Math.cos(ax);
              return { x, y, z };
            }

            function project(p: Pt3, cx: number, cy: number, scaleR: number) {
              const fov = 4;
              const scale = fov / (fov + p.z);
              return {
                sx: cx + p.x * scaleR * scale,
                sy: cy + p.y * scaleR * scale,
                z: p.z,
              };
            }

            function draw() {
              if (!visible) {
                raf = 0;
                return;
              }
              ctx!.clearRect(0, 0, W, H);

              const cx = W / 2;
              const cy = H * 0.48;
              const scaleR = Math.min(W, H) * 0.4;
              const drawn = segments.map((seg) => {
                const pa = project(rotate(seg.a, angleX, angleY), cx, cy, scaleR);
                const pb = project(rotate(seg.b, angleX, angleY), cx, cy, scaleR);
                return { pa, pb, weight: seg.weight, depth: (pa.z + pb.z) / 2 };
              });
              drawn.sort((a, b) => a.depth - b.depth);

              for (const seg of drawn) {
                const alpha = 0.14 + (seg.depth + 1.2) * 0.22;
                ctx!.strokeStyle = `rgba(8, 5, 3, ${Math.min(0.62, Math.max(0.1, alpha))})`;
                ctx!.lineWidth = seg.weight;
                ctx!.beginPath();
                ctx!.moveTo(seg.pa.sx, seg.pa.sy);
                ctx!.lineTo(seg.pb.sx, seg.pb.sy);
                ctx!.stroke();
              }

              if (!reduce) angleY += speedY;
              raf = requestAnimationFrame(draw);
            }

            let raf = 0;
            let visible = true;
            const heroObs = new IntersectionObserver(
              ([entry]) => {
                visible = entry.isIntersecting;
                if (visible && !raf) raf = requestAnimationFrame(draw);
              },
              { threshold: 0 },
            );
            heroObs.observe(canvas);

            if (!reduce) raf = requestAnimationFrame(draw);
            else draw();
          }
        }
      }
    </script>
  </body>
</html>

<style is:global>
  /* ═══════════════════════════════════════════════════════════
     First Crack landing — Optimus-inspired layout
     Colors aligned to project: warm near-black / stone
     ═══════════════════════════════════════════════════════════ */

  html {
    /* Smooth nav handled in JS (custom ease); keep auto to avoid double-smoothing */
    scroll-behavior: auto;
    --ox-scroll-pad: 80px;
    scroll-padding-top: var(--ox-scroll-pad);
  }

  body.ox.is-pane-nav {
    --ox-scroll-pad: 0px;
  }

  /* While nav is animating: disable snap fight + soft content settle */
  body.ox.is-nav-scrolling {
    scroll-snap-type: none;
    pointer-events: none;
  }
  body.ox.is-nav-scrolling .ox-main {
    animation: ox-nav-settle 0.9s cubic-bezier(0.22, 1, 0.36, 1) both;
  }
  @keyframes ox-nav-settle {
    0% {
      opacity: 0.92;
    }
    100% {
      opacity: 1;
    }
  }

  body.ox {
    --ox-bg: #fafaf9;
    --ox-fg: #080503;
    --ox-muted: #5e534a;
    --ox-muted-bg: #ecebe7;
    --ox-border: rgba(8, 5, 3, 0.06);
    --ox-border-strong: rgba(8, 5, 3, 0.12);
    --ox-sans: "Instrument Sans", system-ui, -apple-system, sans-serif;
    --ox-mono: "JetBrains Mono", ui-monospace, monospace;
    --ox-max: 1400px;
    --ox-pad: 24px;

    margin: 0;
    background: var(--ox-bg);
    color: var(--ox-fg);
    font-family: var(--ox-sans);
    font-optical-sizing: auto;
    font-size: 16px;
    line-height: 1.5;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    overflow-x: hidden;
    scroll-snap-type: y proximity;

    /* Override global.css Pretendard tokens on this page */
    --font-display: var(--ox-sans);
    --font-body: var(--ox-sans);
    --font-mono: var(--ox-mono);
  }

  body.ox,
  body.ox button,
  body.ox input,
  body.ox select,
  body.ox textarea {
    font-family: var(--ox-sans);
  }

  body.ox.ox-menu-open {
    overflow: hidden;
  }

  body.ox *,
  body.ox *::before,
  body.ox *::after {
    box-sizing: border-box;
  }

  body.ox a {
    color: inherit;
    text-decoration: none;
  }

  body.ox button {
    font-family: inherit;
    background: none;
    border: none;
    cursor: pointer;
    color: inherit;
  }

  body.ox ul {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .ox-main {
    position: relative;
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* Noise */
  .noise-overlay::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 1;
    opacity: 0.03;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  }

  .ox-shell {
    position: relative;
    z-index: 2;
    max-width: var(--ox-max);
    margin-inline: auto;
    padding-inline: var(--ox-pad);
  }

  /* Full-viewport panes (hero + nav sections) */
  .ox-pane {
    min-height: 100vh;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    scroll-snap-align: start;
    scroll-snap-stop: normal;
  }
  .ox-pane > .ox-shell {
    width: 100%;
  }
  .ox-hero.ox-pane {
    justify-content: center;
  }
  .ox-section.ox-pane,
  .ox-process.ox-pane {
    justify-content: flex-start;
    padding-block: clamp(32px, 5vh, 56px);
  }

  @media (min-width: 1024px) {
    body.ox {
      --ox-pad: 48px;
    }
  }

  /* Reveal */
  .reveal {
    opacity: 0;
    transform: translateY(16px);
    transition:
      opacity 0.7s cubic-bezier(0.22, 1, 0.36, 1),
      transform 0.7s cubic-bezier(0.22, 1, 0.36, 1);
    transition-delay: var(--d, 0ms);
  }
  .reveal-d1 { transition-delay: 80ms; }
  .reveal-d2 { transition-delay: 160ms; }
  .reveal-d3 { transition-delay: 240ms; }
  .reveal.is-in {
    opacity: 1;
    transform: none;
  }

  /* Buttons */
  .ox-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    border-radius: 0;
    font-weight: 500;
    font-size: 14px;
    letter-spacing: -0.01em;
    border: 1px solid transparent;
    transition:
      background 0.2s ease,
      color 0.2s ease,
      border-color 0.2s ease,
      opacity 0.2s ease;
    white-space: nowrap;
  }
  .ox-btn-sm { height: 32px; padding: 0 20px; font-size: 13px; }
  .ox-btn-lg { height: 48px; padding: 0 28px; font-size: 15px; }
  .ox-btn-block { width: 100%; height: 48px; }

  body.ox a.ox-btn-solid,
  body.ox .ox-btn-solid {
    background: var(--ox-fg);
    color: #ffffff;
  }
  body.ox a.ox-btn-solid:hover,
  body.ox .ox-btn-solid:hover {
    opacity: 0.9;
  }

  .ox-btn-outline {
    background: transparent;
    color: var(--ox-fg);
    border-color: var(--ox-border-strong);
  }
  .ox-btn-outline:hover {
    background: rgba(8, 5, 3, 0.03);
    border-color: rgba(8, 5, 3, 0.28);
  }

  /* Header */
  .ox-header {
    position: fixed;
    z-index: 50;
    top: 0;
    left: 0;
    right: 0;
    transition:
      background 0.4s ease,
      border-color 0.4s ease,
      backdrop-filter 0.4s ease,
      transform 0.35s ease;
    border-bottom: 1px solid transparent;
  }
  .ox-header.is-scrolled {
    background: rgba(250, 250, 249, 0.85);
    backdrop-filter: blur(16px);
    border-bottom-color: var(--ox-border);
  }
  .ox-header.is-hidden {
    transform: translateY(-100%);
  }
  .ox-header.is-menu-open {
    transform: none;
  }

  .ox-scroll-progress {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--ox-fg);
    transform: scaleX(0);
    transform-origin: left center;
    opacity: 0.85;
    pointer-events: none;
    z-index: 1;
  }

  .ox-nav {
    max-width: var(--ox-max);
    margin: 0 auto;
    padding: 0 var(--ox-pad);
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    transition: height 0.35s ease;
  }
  .ox-header.is-compact .ox-nav {
    height: 64px;
  }

  .ox-brand {
    display: inline-flex;
    align-items: baseline;
    gap: 6px;
  }
  .ox-brand-name {
    font-family: var(--ox-sans);
    font-size: 1.25rem;
    font-weight: 600;
    letter-spacing: -0.03em;
    line-height: 1;
    transition: font-size 0.35s ease;
  }
  .ox-header.is-compact .ox-brand-name {
    font-size: 1.125rem;
  }

  .ox-nav-links {
    display: none;
    align-items: center;
    gap: 48px;
  }
  @media (min-width: 768px) {
    .ox-nav-links { display: flex; }
  }

  .ox-nav-link {
    position: relative;
    font-size: 14px;
    color: rgba(8, 5, 3, 0.7);
    transition: color 0.25s ease, background 0.25s ease;
  }
  .ox-nav-links .ox-nav-link {
    padding: 6px 12px;
    border-radius: 9999px;
  }
  .ox-nav-link::after {
    content: "";
    position: absolute;
    left: 12px;
    right: 12px;
    bottom: 4px;
    width: auto;
    height: 1px;
    background: var(--ox-fg);
    transform: scaleX(0);
    transform-origin: left center;
    transition: transform 0.3s ease;
  }
  .ox-nav-link:hover {
    color: var(--ox-fg);
  }
  .ox-nav-link:hover::after {
    transform: scaleX(1);
  }
  .ox-nav-link.is-active {
    color: var(--ox-fg);
    background: var(--ox-muted-bg);
  }
  .ox-nav-link.is-active::after {
    transform: scaleX(1);
  }

  .ox-nav-end {
    display: flex;
    align-items: center;
    gap: 16px;
  }
  .ox-nav-signin {
    display: none;
    font-size: 14px;
    color: rgba(8, 5, 3, 0.7);
    transition: color 0.25s ease;
  }
  .ox-nav-signin:hover { color: var(--ox-fg); }
  .ox-nav-email {
    display: none;
    font-size: 12px;
    color: var(--ox-muted);
    max-width: 140px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  @media (min-width: 768px) {
    .ox-nav-signin,
    .ox-nav-email { display: inline; }
    .ox-nav-end .ox-btn { display: inline-flex; }
  }
  @media (max-width: 767px) {
    .ox-nav-end .ox-btn { display: none; }
  }

  .ox-burger {
    display: flex;
    padding: 8px;
  }
  .ox-burger-line {
    transition: transform 0.3s ease, opacity 0.3s ease;
    transform-origin: center;
  }
  .ox-burger[aria-expanded="true"] .ox-burger-line:nth-child(1) {
    transform: translateY(6px) rotate(45deg);
  }
  .ox-burger[aria-expanded="true"] .ox-burger-line:nth-child(2) {
    opacity: 0;
    transform: scaleX(0);
  }
  .ox-burger[aria-expanded="true"] .ox-burger-line:nth-child(3) {
    transform: translateY(-6px) rotate(-45deg);
  }
  @media (min-width: 768px) {
    .ox-burger { display: none; }
  }

  .ox-mobile {
    position: fixed;
    inset: 0;
    z-index: 40;
    background: var(--ox-bg);
    visibility: hidden;
    opacity: 0;
    transform: translateY(-8px);
    pointer-events: none;
    transition: visibility 0.3s ease, opacity 0.3s ease, transform 0.3s ease;
  }
  .ox-mobile.is-open {
    visibility: visible;
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }
  .ox-mobile-inner {
    display: flex;
    flex-direction: column;
    height: 100%;
    padding: 112px 32px 32px;
  }
  .ox-mobile-links {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 24px;
  }
  .ox-mobile-links a {
    font-family: var(--ox-sans);
    font-size: 2.5rem;
    font-weight: 500;
    letter-spacing: -0.03em;
    line-height: 1.1;
    transition: color 0.25s ease;
  }
  .ox-mobile-links a::after {
    display: none;
  }
  .ox-mobile-links .ox-nav-link.is-active {
    background: transparent;
    color: var(--ox-muted);
  }
  .ox-mobile-links a:hover {
    color: var(--ox-muted);
  }
  .ox-mobile-cta {
    display: flex;
    gap: 12px;
    padding-top: 32px;
    border-top: 1px solid var(--ox-border);
  }
  .ox-mobile-cta .ox-btn {
    flex: 1;
    height: 56px;
    font-size: 16px;
  }

  /* Hero */
  .ox-hero {
    position: relative;
    overflow: hidden;
  }

  .ox-grid-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
    opacity: 0.3;
    background-image:
      linear-gradient(var(--ox-border) 1px, transparent 1px),
      linear-gradient(90deg, var(--ox-border) 1px, transparent 1px);
    background-size: 12.5% 12.5%;
  }

  .ox-hero-inner {
    position: relative;
    z-index: 2;
    padding-block: 96px 48px;
    max-width: var(--ox-max);
    display: flex;
    flex-direction: column;
    gap: 40px;
  }

  @media (min-width: 1024px) {
    .ox-hero-inner {
      display: grid;
      grid-template-columns: 1fr 1fr;
      align-items: center;
      gap: 48px;
      padding-block: 88px 40px;
    }
  }

  .ox-hero-copy {
    max-width: 32rem;
  }

  .ox-hero-visual {
    position: relative;
    width: 100%;
    height: min(360px, 42vw);
    max-width: 520px;
    margin-inline: auto;
  }

  @media (min-width: 1024px) {
    .ox-hero-visual {
      height: min(480px, 36vw);
      justify-self: end;
      align-self: start;
      margin-inline: 0;
      margin-top: -16px;
    }
  }

  @media (max-width: 639px) {
    .ox-hero-visual {
      height: 220px;
      margin-top: -4px;
      opacity: 0.45;
    }
  }

  .ox-hero-glow {
    position: absolute;
    inset: 10% 5%;
    border-radius: 50%;
    pointer-events: none;
    opacity: 0.2;
    background: radial-gradient(
      circle,
      rgba(8, 5, 3, 0.07) 0%,
      transparent 68%
    );
  }

  #ox-hero-canvas {
    position: relative;
    z-index: 1;
    display: block;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }

  .ox-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    font-family: var(--ox-mono);
    font-size: 13px;
    color: var(--ox-muted);
    margin-bottom: 24px;
  }
  .ox-line {
    display: block;
    width: 32px;
    height: 1px;
    background: rgba(8, 5, 3, 0.3);
    flex-shrink: 0;
  }
  .ox-line-on-dark {
    background: rgba(250, 250, 249, 0.3);
  }
  .ox-eyebrow-on-dark {
    color: rgba(250, 250, 249, 0.55);
  }

  .ox-hero-title {
    font-family: var(--ox-sans);
    font-weight: 500;
    font-size: clamp(2rem, 4.5vw, 3.5rem);
    line-height: 1.05;
    letter-spacing: -0.04em;
    margin: 0 0 20px;
  }
  .ox-hero-title .block { display: block; }
  .ox-hero-title-muted {
    color: var(--ox-muted);
  }

  .ox-hero-lead {
    font-size: clamp(1rem, 1.25vw, 1.0625rem);
    line-height: 1.65;
    color: var(--ox-muted);
    max-width: 28rem;
    margin: 0 0 20px;
  }

  .ox-hero-trust {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 10px 16px;
    padding-top: 16px;
    margin-bottom: 24px;
    border-top: 1px solid var(--ox-border);
    font-family: var(--ox-mono);
    font-size: 12px;
    color: var(--ox-muted);
  }

  .ox-hero-trust strong {
    font-weight: 600;
    color: var(--ox-fg);
  }

  .ox-hero-trust span + span::before {
    content: "·";
    margin-right: 16px;
    color: rgba(8, 5, 3, 0.25);
  }

  .ox-hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-bottom: 16px;
  }

  .ox-hero-cta-primary {
    min-width: 200px;
  }

  .ox-hero-fine {
    font-size: 13px;
    color: var(--ox-muted);
    opacity: 0.85;
  }

  /* Marquee */
  .ox-marquee {
    position: relative;
    z-index: 2;
    border-top: 1px solid var(--ox-border);
    border-bottom: 1px solid var(--ox-border);
    overflow: hidden;
    mask-image: linear-gradient(90deg, transparent, #000 6%, #000 94%, transparent);
  }
  .ox-marquee-track {
    display: flex;
    gap: 40px;
    width: max-content;
    padding: 20px 0;
    animation: ox-marquee 45s linear infinite;
  }
  @keyframes ox-marquee {
    to { transform: translateX(-50%); }
  }
  .ox-marquee-item {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    font-family: var(--ox-sans);
    font-size: clamp(1rem, 1.8vw, 1.2rem);
    font-weight: 500;
    letter-spacing: -0.02em;
    color: rgba(8, 5, 3, 0.28);
    white-space: nowrap;
    transition: color 0.25s ease;
  }
  .ox-marquee-item:hover {
    color: var(--ox-fg);
  }
  .ox-marquee-dot {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: rgba(8, 5, 3, 0.2);
  }

  /* Proof strip */
  .ox-proof {
    position: relative;
    z-index: 2;
    border-bottom: 1px solid var(--ox-border);
    background: #fff;
  }
  .ox-proof-list {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
    margin: 0;
    padding: 0;
    list-style: none;
  }
  @media (min-width: 900px) {
    .ox-proof-list {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  .ox-proof-item {
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: 24px 20px;
    border-right: 1px solid var(--ox-border);
    border-bottom: 1px solid var(--ox-border);
  }
  @media (min-width: 900px) {
    .ox-proof-item {
      border-bottom: none;
      padding: 28px 24px;
    }
    .ox-proof-item:last-child {
      border-right: none;
    }
  }
  @media (max-width: 899px) {
    .ox-proof-item:nth-child(2n) {
      border-right: none;
    }
    .ox-proof-item:nth-last-child(-n + 2) {
      border-bottom: none;
    }
  }
  .ox-proof-label {
    font-family: var(--ox-mono);
    font-size: 11px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--ox-muted);
  }
  .ox-proof-detail {
    font-family: var(--ox-sans);
    font-size: 15px;
    font-weight: 500;
    letter-spacing: -0.02em;
    color: var(--ox-fg);
  }

  /* Pain */
  .ox-pain {
    border-bottom: 1px solid var(--ox-border);
  }
  .ox-pain .ox-lead {
    max-width: 36rem;
  }
  .ox-pain-grid {
    display: grid;
    gap: 1px;
    background: var(--ox-border);
    border: 1px solid var(--ox-border);
  }
  @media (min-width: 768px) {
    .ox-pain-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  .ox-pain-card {
    background: #fff;
    padding: 28px 24px;
  }
  .ox-pain-n {
    display: block;
    font-family: var(--ox-mono);
    font-size: 12px;
    color: var(--ox-muted);
    margin-bottom: 16px;
  }
  .ox-pain-title {
    font-family: var(--ox-sans);
    font-size: 1.125rem;
    font-weight: 600;
    letter-spacing: -0.025em;
    line-height: 1.3;
    margin: 0 0 10px;
  }
  .ox-pain-body {
    margin: 0;
    font-size: 14px;
    line-height: 1.6;
    color: var(--ox-muted);
  }

  /* Sections */
  .ox-section {
    position: relative;
    z-index: 2;
    padding: 96px 0;
    border-top: 1px solid var(--ox-border);
  }
  @media (min-width: 1024px) {
    .ox-section { padding: 128px 0; }
  }

  .ox-section-head {
    margin-bottom: 64px;
  }
  @media (min-width: 1024px) {
    .ox-section-head { margin-bottom: 96px; }
  }
  .ox-section-head-center {
    text-align: center;
  }

  .ox-h2 {
    font-family: var(--ox-sans);
    font-weight: 500;
    font-size: clamp(2rem, 4vw, 3rem);
    line-height: 1.08;
    letter-spacing: -0.035em;
    margin: 0;
  }
  .ox-muted { color: var(--ox-muted); }
  .ox-h2-on-dark { color: var(--ox-bg); }
  .ox-on-dark-muted { color: rgba(250, 250, 249, 0.45); }

  .ox-lead {
    font-size: 1rem;
    line-height: 1.65;
    color: var(--ox-muted);
    max-width: 36rem;
    margin: 24px 0 0;
  }
  .ox-lead-center {
    margin-inline: auto;
  }

  /* Capabilities list */
  .ox-cap-list {
    display: flex;
    flex-direction: column;
  }

  .ox-cap {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 24px 32px;
    padding: 40px 0;
    border-bottom: 1px solid var(--ox-border);
    align-items: start;
  }
  @media (min-width: 1024px) {
    .ox-cap {
      grid-template-columns: 64px 1fr minmax(240px, 360px);
      gap: 48px;
      padding: 64px 0;
      align-items: center;
    }
  }

  .ox-cap-num {
    font-family: var(--ox-mono);
    font-size: 13px;
    color: var(--ox-muted);
    padding-top: 6px;
  }

  .ox-cap-title {
    font-family: var(--ox-sans);
    font-size: clamp(1.25rem, 2vw, 1.5rem);
    font-weight: 500;
    letter-spacing: -0.025em;
    margin: 0 0 12px;
    line-height: 1.15;
  }

  .ox-cap-text {
    margin: 0;
    font-size: 1rem;
    line-height: 1.65;
    color: var(--ox-muted);
    max-width: 36rem;
  }

  .ox-cap-media {
    grid-column: 1 / -1;
    position: relative;
    aspect-ratio: 16 / 10;
    border: 1px solid var(--ox-border);
    background:
      linear-gradient(145deg, rgba(8, 5, 3, 0.03), transparent 50%),
      #fff;
    border-radius: 0;
  }
  @media (min-width: 1024px) {
    .ox-cap-media {
      grid-column: auto;
      aspect-ratio: 4 / 3;
    }
  }

  .ox-ph-label {
    position: absolute;
    top: 12px;
    left: 12px;
    font-family: var(--ox-mono);
    font-size: 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--ox-muted);
    border: 1px solid var(--ox-border);
    background: rgba(250, 250, 249, 0.9);
    padding: 4px 8px;
  }

  /* Process (dark) */
  .ox-process {
    position: relative;
    z-index: 2;
    background: var(--ox-fg);
    color: var(--ox-bg);
    padding: 96px 0;
  }
  @media (min-width: 1024px) {
    .ox-process { padding: 128px 0; }
  }

  .ox-process-grid {
    display: grid;
    gap: 48px;
  }
  @media (min-width: 1024px) {
    .ox-process-grid {
      grid-template-columns: 1fr 1fr;
      gap: 80px;
      align-items: start;
    }
  }

  .ox-step {
    display: grid;
    grid-template-columns: 40px 1fr;
    gap: 16px;
    width: 100%;
    text-align: left;
    padding: 28px 0;
    border-bottom: 1px solid rgba(250, 250, 249, 0.1);
    opacity: 0.45;
    transition: opacity 0.3s ease;
  }
  .ox-step:hover,
  .ox-step.is-active {
    opacity: 1;
  }
  .ox-step-n {
    font-family: var(--ox-mono);
    font-size: 13px;
    color: rgba(250, 250, 249, 0.45);
    padding-top: 4px;
  }
  .ox-step-title {
    font-family: var(--ox-sans);
    font-size: 1.375rem;
    font-weight: 500;
    letter-spacing: -0.025em;
    margin: 0 0 8px;
    line-height: 1.2;
  }
  .ox-step-body {
    margin: 0;
    font-size: 14px;
    line-height: 1.6;
    color: rgba(250, 250, 249, 0.5);
  }
  .ox-step.is-active .ox-step-body {
    color: rgba(250, 250, 249, 0.65);
  }

  /* Platform */
  .ox-platform {
    display: grid;
    gap: 56px;
  }
  @media (min-width: 1024px) {
    .ox-platform {
      grid-template-columns: 1fr 1fr;
      gap: 80px;
      align-items: start;
    }
  }

  .ox-platform-brands {
    margin: 0 0 28px;
    max-width: 36rem;
    font-size: 14px;
    line-height: 1.55;
    color: var(--ox-muted);
  }

  .ox-platform-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 28px;
  }

  .ox-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin: 40px 0 32px;
  }
  .ox-stat-num {
    font-family: var(--ox-sans);
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    font-weight: 500;
    letter-spacing: -0.03em;
    line-height: 1;
    margin-bottom: 6px;
  }
  .ox-stat-label {
    font-size: 13px;
    color: var(--ox-muted);
  }

  .ox-hw-list {
    border: 1px solid var(--ox-border);
  }
  .ox-hw-item {
    padding: 24px 28px;
    border-bottom: 1px solid var(--ox-border);
    transition: background 0.2s ease;
  }
  .ox-hw-item:last-child { border-bottom: none; }
  .ox-hw-item:hover {
    background: rgba(8, 5, 3, 0.02);
  }
  .ox-hw-tag {
    display: block;
    font-family: var(--ox-mono);
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ox-muted);
    margin-bottom: 8px;
  }
  .ox-hw-item strong {
    display: block;
    font-size: 1.125rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    margin-bottom: 4px;
  }
  .ox-hw-item p {
    margin: 0;
    font-size: 14px;
    color: var(--ox-muted);
  }

  /* Pricing */
  .ox-pricing .ox-section-head {
    margin-bottom: 40px;
  }
  .ox-pricing.ox-pane .ox-section-head {
    margin-bottom: 32px;
  }
  .ox-pricing.ox-pane .ox-bill {
    margin-bottom: 28px;
  }

  /* Features: fit one viewport on desktop */
  @media (min-width: 1024px) {
    #features.ox-pane .ox-section-head {
      margin-bottom: 40px;
    }
    #features .ox-cap-list {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      column-gap: 48px;
      row-gap: 0;
    }
    #features .ox-cap {
      padding: 24px 0;
      border-bottom: none;
      grid-template-columns: 48px 1fr;
      gap: 16px 24px;
      align-items: start;
    }
    #features .ox-cap-media {
      display: none;
    }
  }

  .ox-bill {
    display: flex;
    justify-content: center;
    margin-bottom: 40px;
  }
  .ox-bill-toggle {
    display: inline-flex;
    padding: 4px;
    border: 1px solid var(--ox-border);
    border-radius: 0;
    gap: 2px;
    background: #fff;
  }
  .ox-bill-toggle button {
    padding: 10px 18px;
    border-radius: 0;
    font-size: 13px;
    font-weight: 500;
    color: var(--ox-muted);
    display: inline-flex;
    align-items: center;
    gap: 8px;
    transition: background 0.2s ease, color 0.2s ease;
  }
  .ox-bill-toggle button em {
    font-style: normal;
    font-size: 11px;
    font-weight: 600;
    color: #3f6b2a;
    background: #e8f0df;
    padding: 2px 7px;
    border-radius: 0;
  }
  .ox-bill-toggle button.is-active {
    background: var(--ox-fg);
    color: #ffffff;
  }
  .ox-bill-toggle button.is-active em {
    background: rgba(250, 250, 249, 0.15);
    color: var(--ox-bg);
  }

  .ox-price-grid {
    display: grid;
    gap: 16px;
    max-width: 1100px;
    margin-inline: auto;
  }
  @media (min-width: 900px) {
    .ox-price-grid {
      grid-template-columns: repeat(3, 1fr);
      align-items: stretch;
    }
  }

  .ox-price-card {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 20px;
    padding: 32px 28px;
    border: 1px solid var(--ox-border);
    background: #fff;
  }
  .ox-price-card.is-featured {
    background: var(--ox-fg);
    color: var(--ox-bg);
    border-color: var(--ox-fg);
  }
  .ox-price-badge {
    position: absolute;
    top: -11px;
    left: 28px;
    font-family: var(--ox-mono);
    font-size: 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    background: var(--ox-bg);
    color: var(--ox-fg);
    border: 1px solid var(--ox-border);
    padding: 4px 10px;
  }
  .ox-price-n {
    font-family: var(--ox-mono);
    font-size: 12px;
    color: var(--ox-muted);
    display: block;
    margin-bottom: 12px;
  }
  .ox-price-card.is-featured .ox-price-n {
    color: rgba(250, 250, 249, 0.45);
  }
  .ox-price-top h3 {
    font-family: var(--ox-sans);
    font-size: 1.5rem;
    font-weight: 600;
    letter-spacing: -0.025em;
    margin: 0 0 6px;
  }
  .ox-price-top p {
    margin: 0;
    font-size: 14px;
    color: var(--ox-muted);
  }
  .ox-price-card.is-featured .ox-price-top p {
    color: rgba(250, 250, 249, 0.5);
  }
  .ox-price-amt {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 4px 8px;
  }
  .ox-price-anchor {
    font-size: 14px;
    color: var(--ox-muted);
    margin-right: 4px;
  }
  .ox-price-anchor s {
    text-decoration-thickness: 1px;
  }
  .ox-price-list {
    width: 100%;
    font-size: 14px;
    color: var(--ox-muted);
    text-decoration: line-through;
    opacity: 0.75;
  }
  .ox-price-card.is-featured .ox-price-list {
    color: rgba(250, 250, 249, 0.45);
  }
  .ox-price-num {
    font-family: var(--ox-sans);
    font-size: 2.5rem;
    font-weight: 500;
    letter-spacing: -0.04em;
    line-height: 1;
  }
  @media (min-width: 900px) {
    .ox-price-num {
      font-size: 3rem;
    }
  }
  .ox-price-per {
    font-size: 14px;
    color: var(--ox-muted);
  }
  .ox-price-card.is-featured .ox-price-per {
    color: rgba(250, 250, 249, 0.5);
  }
  .ox-price-note {
    margin: -12px 0 0;
    font-size: 12px;
    color: var(--ox-muted);
  }
  .ox-price-card.is-featured .ox-price-note {
    color: rgba(250, 250, 249, 0.45);
  }
  .ox-price-card ul {
    display: flex;
    flex-direction: column;
    gap: 10px;
    flex: 1;
    padding: 8px 0;
  }
  .ox-price-card li {
    position: relative;
    padding-left: 16px;
    font-size: 14px;
    color: var(--ox-muted);
  }
  .ox-price-card.is-featured li {
    color: rgba(250, 250, 249, 0.6);
  }
  .ox-price-card li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.55em;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: currentColor;
    opacity: 0.5;
  }
  .ox-price-card.is-featured .ox-btn-solid {
    background: var(--ox-bg);
    color: var(--ox-fg);
  }
  .ox-price-card.is-featured .ox-btn-solid:hover {
    opacity: 0.92;
  }

  .ox-price-footnote {
    margin: 32px 0 0;
    text-align: center;
    font-size: 13px;
    line-height: 1.55;
    color: var(--ox-muted);
  }
  .ox-price-footnote a {
    color: var(--ox-fg);
    text-underline-offset: 2px;
  }

  /* FAQ */
  .ox-faq {
    border-top: 1px solid var(--ox-border);
  }
  .ox-faq-list {
    display: flex;
    flex-direction: column;
    border-top: 1px solid var(--ox-border-strong);
    max-width: 48rem;
  }
  .ox-faq-item {
    border-bottom: 1px solid var(--ox-border-strong);
  }
  .ox-faq-item summary {
    cursor: pointer;
    list-style: none;
    padding: 20px 0;
    font-size: 1.0625rem;
    font-weight: 500;
    letter-spacing: -0.02em;
  }
  .ox-faq-item summary::-webkit-details-marker {
    display: none;
  }
  .ox-faq-item summary::after {
    content: "+";
    float: right;
    font-family: var(--ox-mono);
    color: var(--ox-muted);
  }
  .ox-faq-item[open] summary::after {
    content: "−";
  }
  .ox-faq-item p {
    margin: 0 0 20px;
    max-width: 42rem;
    font-size: 15px;
    line-height: 1.65;
    color: var(--ox-muted);
  }
  .ox-faq-more {
    margin: 28px 0 0;
    font-size: 14px;
    color: var(--ox-muted);
  }
  .ox-faq-more a {
    color: var(--ox-fg);
    text-underline-offset: 2px;
  }

  /* Final CTA */
  .ox-final {
    position: relative;
    z-index: 2;
    border-top: 1px solid var(--ox-border);
    padding: 96px 0 112px;
  }
  @media (min-width: 1024px) {
    .ox-final { padding: 128px 0 144px; }
  }
  .ox-final-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    margin-bottom: 24px;
  }
  .ox-final .ox-lead {
    margin-bottom: 32px;
  }

  /* Footer */
  .ox-footer {
    position: relative;
    z-index: 2;
    border-top: 1px solid var(--ox-border);
    padding: 64px 0 32px;
  }
  .ox-footer-top {
    display: grid;
    gap: 48px;
    margin-bottom: 48px;
  }
  @media (min-width: 768px) {
    .ox-footer-top {
      grid-template-columns: 1.2fr 2fr;
    }
  }
  .ox-footer-brand p {
    margin: 16px 0 0;
    font-size: 14px;
    color: var(--ox-muted);
    max-width: 22rem;
  }
  .ox-region {
    margin-top: 20px;
  }
  .ox-region-label {
    display: block;
    font-family: var(--ox-mono);
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ox-muted);
    margin-bottom: 8px;
  }
  .ox-region-links {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .ox-region-links a {
    font-size: 12px;
    padding: 4px 10px;
    border-radius: 9999px;
    border: 1px solid var(--ox-border);
    color: var(--ox-muted);
    text-decoration: none;
  }
  .ox-region-links a.is-active,
  .ox-region-links a:hover {
    color: var(--ox-fg);
    border-color: var(--ox-border-strong);
    background: var(--ox-muted-bg);
  }
  .ox-footer-cols {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
  @media (min-width: 768px) {
    .ox-footer-cols {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  .ox-footer-cols h4 {
    margin: 0 0 16px;
    font-family: var(--ox-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ox-muted);
  }
  .ox-footer-cols a {
    display: block;
    font-size: 14px;
    color: rgba(8, 5, 3, 0.7);
    padding: 5px 0;
    transition: color 0.2s ease;
  }
  .ox-footer-cols a:hover { color: var(--ox-fg); }

  .ox-footer-signout {
    margin: 0;
    padding: 0;
  }

  .ox-footer-signout button {
    display: block;
    padding: 5px 0;
    border: 0;
    background: none;
    font-family: inherit;
    font-size: 14px;
    color: rgba(8, 5, 3, 0.7);
    cursor: pointer;
    transition: color 0.2s ease;
  }

  .ox-footer-signout button:hover {
    color: var(--ox-fg);
  }

  .ox-footer-bottom {
    display: flex;
    justify-content: space-between;
    gap: 16px;
    flex-wrap: wrap;
    padding-top: 24px;
    border-top: 1px solid var(--ox-border);
    font-size: 13px;
    color: var(--ox-muted);
  }

  @media (prefers-reduced-motion: reduce) {
    .ox-header,
    .ox-nav,
    .ox-brand-name,
    .ox-nav-link,
    .ox-nav-link::after,
    .ox-burger-line,
    .ox-mobile {
      transition: none;
    }
    .reveal {
      opacity: 1;
      transform: none;
      transition: none;
    }
    .ox-marquee-track {
      animation: none;
    }
    body.ox.is-nav-scrolling .ox-main {
      animation: none;
    }
  }
</style>
